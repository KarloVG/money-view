import {CurrencyTabularDisplayPipe} from './currency-tabular-display.pipe';

describe('CurrencyTabularDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyTabularDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});

import { CurrencyTabularDisplayPipePipe } from './currency-tabular-display-pipe.pipe';

describe('CurrencyTabularDisplayPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyTabularDisplayPipePipe();
    expect(pipe).toBeTruthy();
  });
});
